<html>
<head>
    <meta charset="utf-8" />
    <meta name="twitter:card" content="summary" />
    <meta property="og:title" content="All-time Bitcoin price chart" />
    <meta property="og:image" content="https://bitcoin.zorinaq.com/price/snippet.png" />
    <title>All-time Bitcoin price chart</title>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/dygraph/2.0.0/dygraph.min.js">
    </script>
</head>
<body>
    <div id="status"></div>
    <div id="graphdiv" style="width: 1200px; max-width: 100%;"></div>
    <script type="text/javascript">
function formatter(y) {
    if (y < 1)
	return '$' + y.toFixed(4);
    else if (y < 1000)
	return '$' + y.toFixed(2);
    else
	return '$' + y.toFixed(0);
}
function formatter2(y) {
    if (y < 1)
	return '$' + y.toFixed(5);
    else
	return '$' + y.toFixed(2);
}

  g = new Dygraph(
    // containing div
    document.getElementById("graphdiv"),
    // data
    [
